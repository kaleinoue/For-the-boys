# Copy this file to ".env" and paste your real key in. NEVER commit the real .env.
# Get a free key (no credit card) at https://aistudio.google.com -> "Get API key"
GEMINI_API_KEY=paste-your-key-here

# Optional: override the model if the default name ever stops working.
# Check current free model names in Google AI Studio.
# FORGE_MODEL=gemini-2.0-flash
